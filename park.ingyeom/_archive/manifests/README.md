# 00d Full Archive Standardization

## Purpose
00d standardized the `park.ingyeom` active and archive layout using full-folder discovery, not only the currently opened zip, notebook, or report files.

## Active Retained
- `data`
- `note.md`
- `notebook/01_data_contract_260513` through `notebook/04_promotion_split_260513`
- `reports/audits/01_data_contract_260513` through `reports/audits/04_promotion_split_260513`
- the current cleanup/discovery review package in `zip`

## Archived
- legacy snapshots
- handoff snapshots
- pre-13b conservative_safe_22 notebooks, reports, figures, models, brief outputs, and old review zips
- preliminary, old, segmentation, and exploratory SHAP references
- previous cleanup, discovery, realignment, and structure logs

## pre13b_conservative_safe_22_reference
This group preserves pre-13b outputs based on the conservative 22-feature reference chain. These files are no longer active canonical outputs, but they remain available as reference assets for copying or review.

## legacy_snapshots
This group preserves the historical `_legacy(260512)` tree as a snapshot. It is archive evidence, not an active modeling chain.

## preliminary_and_legacy_reference
This group preserves preliminary, old, segmentation, and exploratory interpretation traces that should not drive the active canonical chain.

## Next Step
The active modeling chain restarts from `13b_review_feature_resolution_and_sensitivity`.
