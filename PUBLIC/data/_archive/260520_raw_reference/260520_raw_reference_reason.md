# 260520_raw_reference reason

This folder stores the raw-like derived membership reference file that preceded the current model input files.

Reason for isolation:

- This file is not directly consumed by the current model notebooks.
- It may still be useful for lineage checks or regenerating dataset artifacts.
- It is archived instead of deleted because removing raw-reference evidence would make later verification harder.
